# Changelog

## 0.1.3 (2026-09-18)

- **고침**: `.afz` 내보내기가 0.1.2 의 새 파일(`dpat_memory.npz`·`dhist_words.npz`)을 빠뜨려 가져오기가
  "manifest 는 img+maha+dpat+dhist 라는데 파일은 both" 로 거부됐다. 번들 파일 목록을 분기 정의에서
  만들도록 바꿨다. 0.1.2 로 만든 `.afz` 는 0.1.3 에서 다시 내보내면 된다(번들 폴더는 그대로 쓸 수 있다).

## 0.1.2 (2026-09-18)

**"있어야 할 것이 없다"를 본다.** 0.1.1 에서도 breakfast_box 의 "아몬드가 빠지고 바나나칩만 있는"
논리 불량 11장은 어느 분기도 못 봤다(2σ 초과 0~1장). 패치 이상은 *없어진 것* 을 못 보고,
세그멘터 면적은 작은 부품의 양품 변동에 묻히기 때문이다.

- **새 분기 `dhist`** — 양품의 DINOv2 패치를 64개 시각 단어로 묶고 사진마다 단어별 **개수**를 센다.
  "아몬드 단어 = 0" 이 드러난다. breakfast_box 논리 AUROC 85.0 → **97.9**, 그 11장 중 8~11장 검출.
  dpat 이 이미 뽑는 토큰을 그대로 쓰므로 추론 비용은 거의 0, 파일 ~120 KB.
- **선택 후보 교체** — `{struct, +phist, +dpat, +phist+dpat}` → `{struct, +dhist, +dpat, +dpat+dhist}`.
  5품종 실측(불량 10장, 선택에 쓴 이미지는 평가에서 제외): 92.68 → **93.00**, breakfast_box 92.46 → 94.04.
  논리부(`phist`)는 그대로 학습되고 `both` 로 고를 수 있다.
- **검사 탭에 여러 장** — 사진을 여러 장 끌어 놓으면 표(점수·판정·분기별)로 나오고, 줄을 누르면 이상맵.
- 검사 API 가 학습이 고른 조합 이름(`img+maha+dpat`)을 받는다(0.1.1 대시보드에서 막히던 것).

## 0.1.1 (2026-09-17)

**어느 분기를 쓸지 품종마다 정한다.** 5품종 실측에서 고정 `both` 는 4/5 품종에서 구조부 단독보다
낮았다(평균 90.91 vs 92.03). 티칭 때 받은 불량으로 고르면 92.69 다.

- **새 분기 `dpat`** — DINOv2 패치 특징과 양품 메모리의 최근접 거리. 학습이 없고, 양품을 한 번
  훑어 특징을 모아 두는 것이 전부다(10~25초). breakfast_box 85.71 → 92.66.
- **분기 선택** — 불량 8장 이상이면 후보 넷 중에서 고른다(기본값보다 2%p 이상 나을 때만 교체).
  불량이 없으면 구조부 기본값. 선택 근거는 학습 로그의 `[BRANCH]` 줄에 남는다.
- **고침**: 분기가 늘면 0.1.0 번들의 임계값 재계산이 `KeyError` 로 죽던 문제(서버·브라우저 양쪽).
  0.1.0 번들은 그대로 열린다.
- 추론 +20 ms/장 · 번들 +12 MB · `timm` 이 추론 의존성이 됐다(DINOv2).
- 첫 학습 때 DINOv2 가중치 ~85 MB 를 받는다(Apache 2.0). 오프라인 현장은 `weights_root` 복사로.

기각한 후보도 같은 절차로 쟀다: SALAD 의 composition 분기 · 인스턴스 개수 특징 ·
부품별 특징 마할라노비스. 고정 가중 튜닝은 5품종 평균으로는 더 좋아 보였지만(92.96)
품종 교차검증에서 91.72 로 무너졌다.

## 0.1.0 (2026-09-15)

첫 공개. PyPI: `pip install "anofit[train]"` (Python 3.10–3.13, Windows·Linux 휠 8개).

- struct(SALAD) · logic(CSAD) · both 세 모드, 대시보드(준비 → 학습 → 임계값 → 검사)
- `anofit fetch`: 파운데이션 가중치를 원 배포처에서 받고 sha256 대조, 이어받기
- `anofit doctor`: GPU · 디스크 · 의존성 · 가중치 파일 단위 점검
- 번들 `.afz` 내보내기/가져오기, 한 장 점수 `anofit score`
- 90일 평가 라이선스. 만료 뒤에도 학습한 번들로 검사는 계속 됨
- Windows / Linux · Python 3.10 – 3.13
